import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cj373.calculator',
  appName: 'calculator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
